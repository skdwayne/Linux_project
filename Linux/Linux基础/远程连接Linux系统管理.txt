

远程连接Linux系统管理

	为什么要远程连接Linux系统管理
	
	互联网上的计算机都会有一个唯一的32位的地址，IP地址
	我们访问服务器，就必须通过这个ip地址
	局域网里也有预留ip的地址 192/10/172开头。局域网的ip地址也是唯一。
	NAT模式，电脑宿主机的ip在局域网是唯一的，选择了nat模式创建虚拟机，虚拟机就是一个新的局域网
	私有网络
	
	端口的概念
	一台主机，上可能会有很多服务
	一台主机上的不同服务功能，就是通过端口区分，然后让外部人员访问。
	远程连接服务ssh 22
	
	协议的简单概念：
		汉语、英语、法语，不同的服务提供了不同的端口，对应了不同的功能，通信方式可能也是不同的，这种
		通信方式我们就可以理解为协议。
		
		一台主机上的不同服务功能，就是通过端口区分，不同的服务端口，客户端和服务端通信，就可能使用不同的协议。
		ip、port、protocol
		
SSH服务连不上		

如果tenet无法使用    需要进程序和功能 -- 打开过关闭Windows功能开启telnet服务器

排查故障		  
	1.两个机器之间是否通畅，查看物理网络（网线网卡，IP是不是正确）
	ping ip -t 来检测物理网络是否通畅

	2.ssh服务是否好的	
	telnet 	192.168.157.128 22(服务器的ip和端口)
		不通可能原因：	a.服务器端防火墙阻挡
						b.端口没有开放，服务器端没有监听你的连接端口
				netstat -lntup|grep sshd	
				netstat -lntup|grep 22(以ssh服务22端口为例)
		
	ssh重启方法   /etc/init.d/sshd restart 

	crt传送文件到服务器
	yum install lrzsz -y
	
	传送到当前目录
		
		
		
		
		
		
		
		
		
		
	